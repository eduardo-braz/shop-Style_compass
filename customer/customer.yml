spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/db_projeto
        username: postgres
        password: root

    jpa:
        hibernate:
            ddl-auto: update